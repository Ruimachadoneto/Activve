# fitapp — variáveis de ambiente (copie para .env.local e preencha)
# .env.local é ignorado pelo git. NUNCA commite chaves reais.

# Supabase (cliente — seguras de expor no browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase (servidor — NUNCA expor no cliente, só em route handlers/server)
# SUPABASE_SERVICE_ROLE_KEY=
